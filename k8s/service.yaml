apiVersion: v1
kind: Service
metadata:
  name: eightbyte-app-service
  labels:
    app: 8byte-app
spec:
  selector:
    app: 8byte-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer